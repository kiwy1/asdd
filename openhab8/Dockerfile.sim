# Virtual Simulator — оновлює віртуальні Items через OpenHAB REST API
FROM python:3.12-alpine

WORKDIR /app

RUN pip install --no-cache-dir requests

COPY simulator/ ./

ENTRYPOINT ["python", "-u", "simulate.py"]
