apiVersion: 1
providers:
  - name: SmartGrid
    orgId: 1
    folder: SmartGrid
    folderUid: smartgrid
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
