# Ansible inventory - optional for remote deployment
all:
  children:
    openhab_stack:
      hosts:
        localhost:
          ansible_connection: local
      vars:
        project_dir: "{{ playbook_dir }}/.."
        docker_compose_file: "{{ project_dir }}/docker/docker-compose.yml"
